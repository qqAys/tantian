[project]
name = "tantian"
version = "0.1.0"
description = "A privacy-focused, real-time chat application built with NiceGUI."
readme = "README.md"
requires-python = "==3.12.*"
dependencies = [
    "nicegui>=3.3.1",
]
